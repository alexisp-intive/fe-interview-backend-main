import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#4857b7",
    },
    secondary: {
      main: "#ff5b60",
    },
  },
  typography: {
    fontFamily: "Montserrat",
  },
});
